#include <iostream>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    int ans = 1;
    for (int i = 1; i <= b; i++)
    {
        ans = ans * a;
    }
    cout << "a to the power b is: " << ans << endl;

    // int c, d;
    // cin >> c >> d;
    // int ans = 1;
    // for (int i = 1; i <= d; i++)
    // {
    //     ans = ans * c;
    // }
    // cout << "c to the power d is: " << ans << endl;

    int a, b;
    cin >> a >> b;
    int ans = 1;
    for (int i = 1; i <= b; i++)
    {
        ans = ans * a;
    }
    cout << "a to the power b is: " << ans << endl;

    int a, b;
    cin >> a >> b;
    int ans = 1;
    for (int i = 1; i <= b; i++)
    {
        ans = ans * a;
    }
    cout << "a to the power b is: " << ans << endl;
}

//this type of code is bulky and not readable and not easy in maintaince